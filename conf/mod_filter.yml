modules:
  mod_filter:
    access:
      mod_filter:
        allow: all
      mod_filter_presence:
        allow: all
      mod_filter_message:
        allow: all
      mod_filter_iq:
        allow: all
